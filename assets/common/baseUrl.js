import { Platform } from 'react-native'

//this where the main url of the backend source it changes depend on your server and code here the server which has backend code
let baseURL = 'https://courseadminpanel.herokuapp.com/api/v1/';

 

export default baseURL;